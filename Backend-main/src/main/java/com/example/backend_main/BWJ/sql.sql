
-- 비회원 AI 상담: IP + KST 일자별 호출 수 (하루 AI_CONSULT_DAILY_LIMIT회, 회원과 동일 기본 10)
CREATE TABLE TB_AI_GUEST_CONSULT_QUOTA (
    ID         NUMBER GENERATED BY DEFAULT AS IDENTITY PRIMARY KEY,
    CLIENT_IP  VARCHAR2(64) NOT NULL,
    QUOTA_DATE DATE NOT NULL,
    CALL_CNT   NUMBER DEFAULT 0 NOT NULL,
    CONSTRAINT UK_GUEST_AI_QUOTA UNIQUE (CLIENT_IP, QUOTA_DATE)
);


select * from tb_user where user_id ='0321cc';

select * from TB_BOARD;